document.getElementById("startBtn").addEventListener("click", function () {
    const bgMusic = document.getElementById("bgMusic");
    bgMusic.play();
  
    // Redirect to Stage 1 if ready
    // window.location.href = "stage1.html";
  });
 